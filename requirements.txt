Flask==3.1.3
gunicorn==23.0.0
# Optional: used only by the Vertex JourneyEvent extractor (VertexGeminiJourneyExtractor).
# Authenticates via the Cloud Run service account (ADC) — no API key. The extractor
# stays dormant until MIZOKI_GEMINI_PROJECT / GOOGLE_CLOUD_PROJECT is set.
google-genai==2.10.0

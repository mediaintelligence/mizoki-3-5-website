# Node / Vite
node_modules/
dist/
.vite/
*.log
npm-debug.log*

# Local env
.env
.env.local
.env.*.local

# OS / IDE
.DS_Store
.vscode/
.idea/

# Terraform — keep main.tf and the example; ignore state and locals.
infrastructure/.terraform/
infrastructure/.terraform.lock.hcl
infrastructure/terraform.tfstate
infrastructure/terraform.tfstate.*
infrastructure/terraform.tfvars
infrastructure/*.tfplan
